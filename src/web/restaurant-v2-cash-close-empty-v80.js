/* VANTIX_RESTAURANT_V2_CASH_CLOSE_EMPTY_V80_1 */
(()=>{'use strict';
const MARKER='VANTIX_RESTAURANT_V2_CASH_CLOSE_EMPTY_V80_1';
const SESSION_KEY='vantixgc_core_session_v1';
let timer=null;
let epoch=0;
let observer=null;
let closing=false;

function session(){try{return JSON.parse(localStorage.getItem(SESSION_KEY)||'null')}catch{return null}}
function headers(){const s=session();if(!s?.token||!s?.subdomain)return null;return{'Content-Type':'application/json',Authorization:`Bearer ${s.token}`,'x-tenant-subdomain':s.subdomain}}
function selectedTableId(){return document.querySelector('#queue [data-table].active')?.dataset?.table||null}
function paymentCard(){return document.querySelector('#detail .payment-card')}
function detailVisible(){const node=document.querySelector('#detail');return Boolean(node&&!node.hidden)}
function notice(text,error=false){const node=document.querySelector('#notice');if(!node)return;node.textContent=String(text||'');node.classList.toggle('error',Boolean(error))}
function removePanel(){document.querySelector('[data-v80-empty-close]')?.remove();const payment=paymentCard();if(payment)payment.hidden=false}
function buttonMarkup(tableId,name){return `<section class="payment-card" data-v80-empty-close="1" style="border-color:#f0b8a8;background:#fffaf8"><div class="section-title"><div><small>MESA SIN CONSUMO</small><h3>Cerrar mesa abierta por error</h3></div><span class="pos-badge">$ 0</span></div><p class="fiscal-note" style="font-size:13px;line-height:1.45;margin:0 0 14px">${name?`<b>${String(name).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}</b> · `:''}La cuenta fue solicitada pero no tiene productos. Se puede liberar sin registrar un cobro.</p><button class="rv2-btn rv2-btn-primary charge-btn" data-v80-close-empty="${String(tableId).replace(/"/g,'&quot;')}" type="button" style="background:#c2410c;border-color:#c2410c">CERRAR MESA SIN CONSUMO</button><p class="fiscal-note">El servidor volverá a validar que no existan pedidos enviados, pagos ni documentos antes de cerrar.</p></section>`}
function showPanel(tableId,detail){const payment=paymentCard();if(!payment)return;let panel=document.querySelector('[data-v80-empty-close]');if(!panel){payment.insertAdjacentHTML('beforebegin',buttonMarkup(tableId,detail?.table?.name||detail?.table?.code||''));panel=document.querySelector('[data-v80-empty-close]')}const button=panel?.querySelector('[data-v80-close-empty]');if(button)button.dataset.v80CloseEmpty=tableId;payment.hidden=true}
async function refreshEligibility(){const tableId=selectedTableId();const auth=headers();const myEpoch=++epoch;if(!tableId||!auth||!detailVisible()||!paymentCard()){removePanel();return}try{const response=await fetch(`/api/v1/restaurante/mesas/${encodeURIComponent(tableId)}/detalle-v67`,{method:'GET',cache:'no-store',headers:auth});let body=null;try{body=await response.json()}catch{}if(myEpoch!==epoch||selectedTableId()!==tableId)return;const detail=body?.data||null;const requested=String(detail?.session?.state||'').toUpperCase()==='CUENTA_PEDIDA';const zero=Number(detail?.sale?.total||0)===0;const noProducts=Array.isArray(detail?.items)&&detail.items.length===0;if(!response.ok||!requested||!zero||!noProducts){removePanel();return}showPanel(tableId,detail)}catch{if(myEpoch===epoch)removePanel()}}
function schedule(delay=120){if(timer)clearTimeout(timer);timer=setTimeout(()=>{timer=null;refreshEligibility().catch(()=>{})},delay)}
async function closeEmpty(button,tableId){if(closing||!tableId||selectedTableId()!==tableId)return;if(!confirm('La mesa está en $0 y no tiene productos. ¿Cerrar la mesa sin consumo?'))return;const auth=headers();if(!auth)return notice('La sesión de Caja perdió la autenticación.',true);closing=true;button.disabled=true;button.textContent='CERRANDO MESA…';try{const response=await fetch(`/api/v1/restaurante/v2/caja/mesas/${encodeURIComponent(tableId)}/cerrar-vacia-v80`,{method:'POST',cache:'no-store',headers:auth,body:'{}'});let body=null;try{body=await response.json()}catch{}if(!response.ok)throw new Error(body?.error?.message||body?.message||'No fue posible cerrar la mesa sin consumo.');notice('Mesa cerrada sin consumo. No se registró ningún cobro.');removePanel();setTimeout(()=>location.reload(),220)}catch(error){button.disabled=false;button.textContent='CERRAR MESA SIN CONSUMO';notice(error?.message||'No fue posible cerrar la mesa sin consumo.',true);schedule(250)}finally{closing=false}}
document.addEventListener('click',event=>{const button=event.target?.closest?.('[data-v80-close-empty]');if(button){event.preventDefault();event.stopPropagation();closeEmpty(button,button.dataset.v80CloseEmpty).catch(()=>{});return}if(event.target?.closest?.('#queue [data-table]')||event.target?.closest?.('#refresh'))schedule(180)},true);
window.addEventListener('vantix:tenant-realtime',()=>schedule(180));
window.addEventListener('pageshow',()=>schedule(100));
function start(){if(observer)return;const root=document.querySelector('.cash-shell')||document.body;observer=new MutationObserver(()=>schedule(140));observer.observe(root,{childList:true,subtree:true,attributes:true,attributeFilter:['class','hidden','data-table']});document.documentElement.dataset.cashCloseEmptyV80='1';schedule(80)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();
window.VantixGCCashCloseEmptyV80=Object.freeze({marker:MARKER,backend:'/v2/caja/mesas/:tableId/cerrar-vacia-v80',accountRequestedOnly:true,zeroAndNoProducts:true,cashierAllowed:true,noPaymentSideEffects:true});
})();
