model RestaurantCompanyProfile {
  id           String   @id @default(uuid())
  tenantId     String   @unique
  address      String?
  city         String?
  department   String?
  phone        String?
  email        String?
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  @@index([tenantId])
}
