name: Restaurant Demo Final Live Read V2

on:
  pull_request:
    branches:
      - main

jobs:
  read-production:
    runs-on: ubuntu-latest
    steps:
      - name: Read live Restaurant demo readiness
        shell: bash
        run: |
          set -euo pipefail
          echo '=== DEMO READINESS ==='
          curl -fsSL --max-time 20 https://core.vantixgc.com/api/public/restaurante/demo-readiness
          echo
          echo '=== CORE STATUS ==='
          curl -fsSL --max-time 20 https://core.vantixgc.com/api/v1/status
