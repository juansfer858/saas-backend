# Acceptance V104

Accepted when a newly emitted table sale followed by a newly emitted delivery sale receive consecutive numeric POS numbers with no duplicate, and existing historical `FV-*` documents remain unchanged.
